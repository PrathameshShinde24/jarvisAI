# ── Core runtime ──────────────────────────────────────────
groq>=0.9.0                  # Groq API — free tier, Llama 3.3 70B with tool use
fastapi>=0.111.0             # API server + WebSocket
uvicorn[standard]>=0.29.0   # ASGI server
python-dotenv>=1.0.0        # .env loading
websockets>=12.0

# ── Wake trigger ──────────────────────────────────────────
keyboard>=0.13.5            # Global hotkey listener (Ctrl+Space activates Jarvis)
numpy>=1.24.0               # Audio RMS calculation

# ── Audio capture & playback ──────────────────────────────
pyaudio>=0.2.14             # Mic input (on Windows: pip install pipwin && pipwin install pyaudio)
pygame>=2.5.0               # TTS audio playback (cross-platform)

# ── Speech-to-text ────────────────────────────────────────
faster-whisper>=1.0.0       # Local Whisper ("base" model — ~150 MB, fully free)

# ── Text-to-speech ────────────────────────────────────────
edge-tts>=6.1.9             # Microsoft Edge TTS (free, no API key needed)

# ── Long-term memory ──────────────────────────────────────
chromadb>=0.5.0             # Local vector database (free, no external service)
sentence-transformers>=3.0.0  # Embeddings for ChromaDB

# ── Web tools (Phase 4) ───────────────────────────────────
playwright>=1.44.0          # Browser automation
httpx>=0.27.0               # Async HTTP client
beautifulsoup4>=4.12.0      # HTML parsing

# ── Windows system control ────────────────────────────────
pycaw>=20230407             # Windows Core Audio API (volume control)
comtypes>=1.4.0             # Required by pycaw

# ── Google APIs (Phase 5) ─────────────────────────────────
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0
